{
  "name": "@alzuma/tools",
  "version": "1.0.2",
  "description": "",
  "main": "lib/index",
  "types": "lib/index",
  "scripts": {
    "build": "npm t & npm run format & npm run lint && npm run build:types && webpack --mode production",
    "build:types": "tsc --emitDeclarationOnly",
    "test": "jest",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.tsx\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "release": "rimraf dist && npm run build && npm version patch && git push && git push --tags && npm publish"
  },
  "keywords": [],
  "author": "Kristaps Vītoliņš",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.2.2",
    "@babel/plugin-proposal-class-properties": "^7.2.3",
    "@babel/plugin-transform-arrow-functions": "^7.2.0",
    "@babel/preset-env": "^7.2.3",
    "@babel/preset-typescript": "^7.1.0",
    "@types/jest": "^23.3.10",
    "babel-loader": "^8.0.4",
    "jest": "^23.6.0",
    "prettier": "^1.15.3",
    "rimraf": "^2.6.3",
    "ts-jest": "^23.10.5",
    "tslint": "^5.12.0",
    "tslint-config-prettier": "^1.17.0",
    "typescript": "^3.2.2",
    "webpack": "^4.28.2",
    "webpack-cli": "^3.1.2"
  },
  "dependencies": {
    "typescript-optional": "^1.8.0"
  }
}
